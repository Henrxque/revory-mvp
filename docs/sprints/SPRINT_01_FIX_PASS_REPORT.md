# REVORY — Sprint 01 Fix Pass Report

## Issues Fixed

### 1. Clean rerun script still used old Seller-era onboarding selectors

- Source: `SPRINT_01_QA_BUG_HUNT_REPORT.md`.
- Problem: `npm run qa:clean-rerun` previously failed because it waited for the old label `Value tied to one booked appointment`.
- Fix:
  - Updated the onboarding field selector to `Estimated appointment value`.
  - Updated activation selector from old Seller language to `Activate REVORY read`.
  - Removed a remaining `go live with seller` fallback matcher from the shared continue helper.
- Result: the QA script now follows the Sprint 01 revenue-leak positioning and no longer depends on legacy Seller copy.

### 2. Landing email link pointed to a broken Cloudflare-style route

- Source: `SPRINT_01_QA_BUG_HUNT_REPORT.md`.
- Problem: the rendered landing page exposed an `Email us` link pointing to `/cdn-cgi/l/email-protection...`, which returned `404`.
- Fix:
  - Replaced the landing reference email link with `mailto:hello@revory.com`.
  - Repaired footer legal/contact links in the landing reference.
  - Kept defensive normalization in the landing adapter so any future `/cdn-cgi/l/email-protection...` reference is rendered as `mailto:hello@revory.com`.
- Result: public link smoke no longer finds the broken `/cdn-cgi` route.

### 3. Smoke LLM scripts still carried REVORY Seller / booking-first prompt language

- Source: follow-up validation during this fix pass.
- Problem: internal smoke scripts still described the product as `REVORY Seller` and `booking-first`.
- Fix:
  - Updated prompts to `REVORY Revenue Leak Detector`.
  - Reframed guardrails as evidence-first / revenue-risk-first.
  - Added explicit no-invented-revenue, no-confirmed-loss, no-recovery-causality, no CRM/inbox/BI/scheduling/sales-agent/clinical-advisor language.
- Result: internal QA scripts no longer preserve the old public product promise.

## Files Changed

- `scripts/run-clean-rerun.mjs`
- `scripts/smoke-llm-bounded-runtime.mjs`
- `scripts/qa-llm-fallback-structured-output.mjs`
- `src/app/page.tsx`
- `src/content/revory-landing-reference.html`
- `docs/sprints/SPRINT_01_FIX_PASS_REPORT.md`

## Issues Intentionally Not Fixed

### Historical Seller documentation

- Decision: not changed in this pass.
- Reason: older Seller docs are intentionally preserved as historical implementation substrate and already carry source-of-truth disclaimers.
- Risk: future contributors could still copy outdated language if they ignore `docs/source-of-truth.md`.
- Recommendation: keep using `docs/source-of-truth.md` as the current product reference.

### Transitional booking substrate

- Decision: not removed or renamed at model/route level.
- Reason: Sprint 01 is copy/positioning alignment only. Renaming models/routes would expand scope and risk breaking working functionality.
- Risk: naming can look old internally, but visible copy now frames it as booking-risk/evidence/action guidance.

### Large embedded base64 logo in landing reference

- Decision: not cleaned now.
- Reason: not a functional bug, not a public promise issue, and not required for Sprint 01 readiness.
- Risk: makes diffs and grep audits noisy.
- Recommendation: consider asset cleanup later as a technical hygiene pass, not as Sprint 01 scope.

### Defensive `cdn-cgi` regex in `src/app/page.tsx`

- Decision: kept intentionally.
- Reason: it prevents future imported/reference HTML from rendering a broken email-protection link.
- Classification: not a broken public link; it is a safety normalization.

## Tests Run

- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm run build` — passed.
- `npm run env:check` — passed.

Additional validation:

- Searched active app/components/services/scripts/landing source for:
  - `REVORY Seller`
  - `Clone your best seller`
  - `booking acceleration software`
  - `AI sales assistant`
  - `Revenue Generated by Revory`
  - stale onboarding labels
  - broken `/cdn-cgi/l/email-protection` links
- Only remaining `cdn-cgi` occurrence is the defensive normalization in `src/app/page.tsx`.

## Final Status

**PASS.**

All valid Sprint 01 QA issues have been fixed without implementing new product scope, without adding a leak engine, without changing billing logic, and without creating new product surfaces.

Sprint 01 is now clean from a functional QA and copy-positioning fix-pass standpoint. The remaining risks are historical-doc/reference risks, not active product defects.
