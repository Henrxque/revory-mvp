import Link from "next/link";

export default function AiDisclosurePage() { return <main className="min-h-screen bg-[color:var(--background)] px-5 py-8 text-[color:var(--foreground)]"><section className="rev-card-premium mx-auto max-w-3xl rounded-[30px] p-6 md:p-9"><p className="rev-kicker">AI disclosure</p><h1 className="mt-3 text-[clamp(2.2rem,5vw,4rem)] font-semibold leading-[.95] tracking-[-.06em]">Optional assistance, deterministic financial logic.</h1><div className="mt-7 space-y-4 text-sm leading-7 text-[color:var(--text-muted)]"><p>REVORY may use bounded AI to suggest column mappings, classify ambiguous text as a review candidate, or explain deterministic evidence. Human confirmation is required before assisted mappings persist.</p><p>Core eligibility, matching, financial calculations, totals, workspace authorization and Data Quality suppression do not depend on AI. Turning AI off must leave the deterministic result unchanged.</p><p>The mapping provider boundary receives minimized schema/profile information, not complete customer rows. AI may not prove approval, performed work, accounting loss or recovered revenue, and it does not send follow-ups.</p></div><Link className="rev-action-button mt-8 inline-flex" href="/">Back to REVORY</Link></section></main>; }
