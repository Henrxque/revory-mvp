import Link from "next/link";
import { notFound,redirect } from "next/navigation";

import { RevoryStatusBadge } from "@/components/ui/RevoryStatusBadge";
import { getAppContext } from "@/services/app/get-app-context";
import { buildSignInRedirectPath } from "@/services/auth/redirects";
import { getQuoteRecoveryFindingDetail } from "@/services/quote-recovery/read-model";
import { acknowledgeQuoteRecoveryFinding,dismissQuoteRecoveryFinding,recoverQuoteRecoveryFinding,resolveQuoteRecoveryFinding } from "../actions";

function label(value:string){return value.toLowerCase().split("_").map((part)=>part[0].toUpperCase()+part.slice(1)).join(" ")}
function money(cents:number){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(cents/100)}
export default async function FindingDetailPage({params}:{params:Promise<{id:string}>}){const context=await getAppContext();if(!context)redirect(buildSignInRedirectPath("/app/revenue-leaks"));const{id}=await params;const finding=await getQuoteRecoveryFindingDetail(context.workspace.id,id);if(!finding)notFound();const evidence=Array.isArray(finding.evidenceJson)?finding.evidenceJson as Array<{field:string;value:string|number|null;provenance?:{fileName?:string;rowNumber?:number}}>:[];return <div className="space-y-6"><Link className="text-sm font-semibold text-[color:var(--accent-light)]" href="/app/revenue-leaks">← Back to opportunities</Link><section className="rev-shell-hero rev-accent-mist rounded-[30px] p-6 md:p-8"><div className="flex flex-wrap justify-between gap-5"><div className="max-w-3xl"><div className="flex flex-wrap gap-2"><RevoryStatusBadge tone="accent">{label(finding.findingType)}</RevoryStatusBadge><RevoryStatusBadge tone="neutral">{label(finding.status)}</RevoryStatusBadge></div><h1 className="mt-4 text-3xl font-bold tracking-[-.04em]">Estimate {finding.estimateExternalId}</h1><p className="mt-3 text-sm leading-7 text-[color:var(--text-muted)]">{finding.reason}</p></div><div><p className="text-2xl font-bold">{finding.valueCents===null?"Operational risk":money(finding.valueCents)}</p><p className="mt-1 text-xs text-[color:var(--text-muted)]">{label(finding.valueBasis)} · {label(finding.confidence)} confidence</p>{finding.disposition==="RECOVERED"?<p className="mt-2 text-sm font-bold text-[color:var(--success)]">Customer-confirmed recovered: {money(finding.recoveredValueCents??0)}</p>:null}</div></div></section><section className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_340px]"><div className="rev-shell-panel rounded-[26px] p-5"><p className="rev-label">Source lineage</p><h2 className="mt-2 text-xl font-bold">Evidence used by this rule</h2><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-xs uppercase tracking-wider text-[color:var(--text-subtle)]"><tr><th className="px-3 py-2">Field</th><th className="px-3 py-2">Value</th><th className="px-3 py-2">Source</th></tr></thead><tbody>{evidence.map((item)=><tr className="border-t border-[color:var(--border)]" key={`${item.field}-${item.provenance?.rowNumber}`}><td className="px-3 py-3 font-bold">{label(item.field)}</td><td className="px-3 py-3 text-[color:var(--text-muted)]">{String(item.value??"Not supplied")}</td><td className="px-3 py-3 text-[color:var(--text-muted)]">{item.provenance?.fileName??"Imported source"}{item.provenance?.rowNumber?` · row ${item.provenance.rowNumber}`:""}</td></tr>)}</tbody></table></div></div><aside className="rev-shell-panel rounded-[26px] p-5"><p className="rev-label">Recommended review</p><p className="mt-3 text-sm leading-6 text-[color:var(--text-muted)]">{finding.recommendedAction}</p><div className="mt-5 grid gap-2"><form action={acknowledgeQuoteRecoveryFinding.bind(null,finding.id)}><button className="rev-button-secondary w-full">Acknowledge</button></form><form action={resolveQuoteRecoveryFinding.bind(null,finding.id)}><button className="rev-button-primary w-full">Mark reviewed</button></form><form action={recoverQuoteRecoveryFinding.bind(null,finding.id)} className="rounded-2xl border border-[color:var(--border-accent)] p-3"><label className="text-xs font-bold" htmlFor="recoveredValue">Confirmed recovered value (USD)</label><input className="rev-input-field mt-2 w-full" id="recoveredValue" min="0" name="recoveredValue" placeholder="0" step="0.01" type="number"/><button className="rev-button-primary mt-2 w-full">Mark recovered</button></form><form action={dismissQuoteRecoveryFinding.bind(null,finding.id)}><button className="rev-button-secondary w-full">Dismiss false positive</button></form></div><p className="mt-4 text-xs leading-5 text-[color:var(--text-subtle)]">Recovered value is customer-confirmed and remains separate from the original estimated opportunity.</p></aside></section></div>}
