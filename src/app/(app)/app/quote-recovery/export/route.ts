import { getAppContext } from "@/services/app/get-app-context";
import { getQuoteRecoveryRead } from "@/services/quote-recovery/read-model";

function safe(value: unknown) { const text = String(value ?? ""); const guarded = /^[=+\-@]/.test(text) ? `'${text}` : text; return `"${guarded.replaceAll('"','""')}"`; }
export async function GET() { const context = await getAppContext(); if (!context) return new Response("Unauthorized", { status: 401 }); const read = await getQuoteRecoveryRead(context.workspace.id); const headers = ["finding_type","estimate_external_id","status","severity","confidence","value_basis","value_cents","currency","reason","recommended_action"]; const rows = read.findings.map((finding) => [finding.findingType,finding.estimateExternalId,finding.status,finding.severity,finding.confidence,finding.valueBasis,finding.valueCents ?? "",finding.currency,finding.reason,finding.recommendedAction].map(safe).join(",")); return new Response([headers.join(","),...rows].join("\r\n"), { headers: { "Content-Type": "text/csv; charset=utf-8", "Content-Disposition": `attachment; filename="revory-quote-recovery-${new Date().toISOString().slice(0,10)}.csv"`, "Cache-Control": "private, no-store" } }); }
