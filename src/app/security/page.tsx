import Link from "next/link";

export default function SecurityPage() {
  return <main className="min-h-screen bg-[color:var(--background)] px-5 py-8 text-[color:var(--foreground)]"><section className="rev-card-premium mx-auto max-w-3xl rounded-[30px] p-6 md:p-9"><p className="rev-kicker">Security overview</p><h1 className="mt-3 text-[clamp(2.2rem,5vw,4rem)] font-semibold leading-[.95] tracking-[-.06em]">How REVORY protects evidence.</h1><div className="mt-7 space-y-4 text-sm leading-7 text-[color:var(--text-muted)]"><p>Every active read, write, export and finding mutation is authorized against a workspace. Canonical records preserve source-system IDs and provenance; ambiguous links suppress financial output.</p><p>Uploads are limited by file, row and batch policy. REVORY accepts CSV/XLSX only, checks file signatures and XLSX archive expansion, rejects workbook formulas and uses atomic persistence with active snapshot boundaries.</p><p>Sessions use managed OAuth or password authentication with durable login throttling and versioned revocation. Billing webhooks require a Stripe signature and a persistent replay ledger.</p><p>Data is encrypted in transit by the hosting platform and at rest by managed storage providers. Secrets belong in managed environment variables, never in uploads or source control.</p><p>REVORY does not claim SOC 2, ISO 27001, PCI certification or any other certification not independently obtained. A qualified security review, production monitoring, restore drill and public vulnerability-reporting channel remain required before Pro paid beta.</p></div><Link className="rev-action-button mt-8 inline-flex" href="/">Back to REVORY</Link></section></main>;
}
