export type MovementFinding={fingerprint:string;severity:string;valueCents:number|null};
const severityRank:Record<string,number>={LOW:1,MEDIUM:2,HIGH:3,CRITICAL:4};
export function classifyQuoteRecoveryMovement(current:MovementFinding[],previous:MovementFinding[]){const currentBy=new Map(current.map((item)=>[item.fingerprint,item])),previousBy=new Map(previous.map((item)=>[item.fingerprint,item]));let newCount=0,persistentCount=0,worseningCount=0,resolvedCount=0;for(const item of current){const before=previousBy.get(item.fingerprint);if(!before)newCount+=1;else{persistentCount+=1;if((item.valueCents??0)>(before.valueCents??0)||(severityRank[item.severity]??0)>(severityRank[before.severity]??0))worseningCount+=1}}for(const item of previous)if(!currentBy.has(item.fingerprint))resolvedCount+=1;return{newCount,persistentCount,worseningCount,resolvedCount}}
